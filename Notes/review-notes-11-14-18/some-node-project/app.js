const axios = require('axios')
// Only works in a node project
// either only running on the console,
// or being compiled before running in
// the browser. 

node package manager
    - npm init -y
    - npm install --save-dev jest


